{% extends 'base.html.twig' %}

{% block title %}Reservationevenement{% endblock %}

{% block body %}
    <h1>Reservationevenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservationevenement.id }}</td>
            </tr>
            <tr>
                <th>Id_event</th>
                <td>{{ reservationevenement.idEvent }}</td>
            </tr>
            <tr>
                <th>Id_user</th>
                <td>{{ reservationevenement.idUser }}</td>
            </tr>
            <tr>
                <th>Date_reservation</th>
                <td>{{ reservationevenement.dateReservation ? reservationevenement.dateReservation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservationevenement_index') }}">back to list</a>

    <a href="{{ path('reservationevenement_edit', {'id': reservationevenement.id}) }}">edit</a>

    {{ include('reservationevenement/_delete_form.html.twig') }}
{% endblock %}
