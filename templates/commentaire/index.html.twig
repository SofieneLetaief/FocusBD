{% extends 'admin.html.twig' %}

{% block title %}Commentaire index{% endblock %}

{% block content %}
    <h1>Commentaire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Contenu</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commentaire in commentaires %}
            <tr>
                <td>{{ commentaire.id }}</td>
                <td>{{ commentaire.contenu }}</td>
                <td>
                    <a href="{{ path('commentaire_show', {'id': commentaire.id}) }}" class="btn btn-primary btn">Afficher</a>
                    <a href="{{ path('commentaire_edit', {'id': commentaire.id}) }}" class="btn btn-primary btn">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('commentaire_new') }}" class="btn btn-primary btn">Créer</a>
{% endblock %}
